/**
 * Input types
 */
// Would I really have these here? Or just import what I need in Commands?
// Commands are handled by the implementtion...
export type ValidatedFile = {
  readonly fileID: string;
  readonly fileName: string;
  readonly filePath: string;
};

/**
 * Output types
 */

