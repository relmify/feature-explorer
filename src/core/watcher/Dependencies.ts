// import { ReplaySubject } from 'rxjs';

// export type WatcherDependencies = {
//  fileSystemWatcher: FileSystemWatcher;
// };

// export const WatcherConfig$ = new ReplaySubject<WatcherDependencies>();
// export const WatcherConfig = WatcherConfig$.subscribe({ next: (dependencies: WatcherDependencies) => });

// export type RootDirectory = string;
// const workspaceRoot: WorkspaceRoot = workspace.workspaceFolders ? workspace.workspaceFolders[0].uri.path : '';
