/**
 * @packageDocumentation
 * The Validator services validates feature file contents to highlight content that
 * appears to be incomplete.
 */

// import { Either } from 'fp-ts/lib/Either';
// import { ParsedFile, ValidatedFile } from './Model';

/**
 * Input - Commands
 *
 * The SummarizingService application service is the one that listens for
 *
 */

// Behaviors

// export const validateFile = (parsedFile: ParsedFile): Either<ValidationError, ValidatedFile> => {};
