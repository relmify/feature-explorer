import { Event, EventHandler } from '../../../framework/eventBus';

export const startFileWatchHandler: EventHandler = (event: Event): readonly Event[] => {
  return [];
};

export const stopFileWatchHandler: EventHandler = (event: Event): readonly Event[] => {
  return [];
};
